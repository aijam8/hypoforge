#!/usr/bin/env bash
# HypoForge launcher — runs the CLI from the project venv.
DIR="$(cd "$(dirname "$0")" && pwd)"
PY="$DIR/.venv/bin/python"
[ -x "$PY" ] || PY="python3"
exec "$PY" -m hypoforge "$@"
